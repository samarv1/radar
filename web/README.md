# Frontend

Next.js app for the [Startup Sourcing Tool](../README.md). Reads directly from the shared Postgres database via server components — no API layer in between.

```bash
npm install
npm run dev
```

See the [root README](../README.md) for env var setup and how the data gets there.
